# String

The string module is the module to manipulate strings.

## Competences

- Copy a string
- Tokenize a string given a delimitator char
- parse an int into a str
- parse a string into an int
- get first char at
- get last char at
- contains substring
- count the instances of char in a string
- size
- to lower case
- to apper case
